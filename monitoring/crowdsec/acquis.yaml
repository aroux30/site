# CrowdSec log acquisition configuration
# Reads Nginx access/error logs and FastAPI backend security audit logs

filenames:
  - /var/log/nginx/access.log
  - /var/log/nginx/error.log
labels:
  type: nginx
---
filenames:
  - /var/log/app/backend_security.log
labels:
  type: syslog
