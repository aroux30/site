# Cross-System Bug Matrix — ARoux30 / SITE

> Audit date: 2026-09-11. Only **reproducible, code-verified** issues are listed.
> Priority: P0 = security / money / catastrophic data / production blocker · P1 = broken customer journey · P2 = inconsistency / operational risk · P3 = polish / debt.
> "FIXED" entries were repaired in this audit and covered by regression tests (`backend/tests/unit/test_payment_hardening.py`); "OPEN" entries are documented with recommended fixes.

| ID | Area | Priority | Trigger | Expected | Actual (root cause) | Affected systems | Fix | Regression test | Verification |
|---|---|---|---|---|---|---|---|---|---|
| BUG-BE-01 | Payments/Wallet | **P0** | Customer selects "کیف پول" at checkout | Wallet is debited; order confirmed only after a real debit | `WalletPaymentProvider.verify_payment` returned success and **never debited the wallet** (providers/wallet.py:57-80). Any order could be confirmed for free. | payments, wallet, orders, finance | Wallet debit now executes inside the locked payment-verification transaction (`payment_service.verify_payment`); failed debit → payment FAILED, order stays PENDING. Balance pre-check at create. | `test_wallet_verify_debits_wallet_exactly_once`, `test_wallet_verify_insufficient_balance_fails_payment` | **FIXED** ✅ |
| BUG-BE-02 | Wallet | **P0** | `POST /wallet/deposit` | Top-up only after a completed payment | Endpoint credited real balance with **no payment at all** (wallet/api/routes.py:73-98) — direct money printing. Combined with checkout = full revenue bypass. | wallet, payments, orders | Fail-closed: HTTP 403 in production; dev/testing unchanged. | `test_wallet_deposit_disabled_in_production` | **FIXED** ✅ |
| BUG-BE-03 | Payments | **P0** | `POST /payments` with arbitrary `amount` | Amount must equal order total; only owner can pay; only PENDING payable | Client-supplied amount **never compared to the order**; no ownership check; non-PENDING orders payable (payment_service.create_payment). Enabled paying 1 IRR for a 10M IRR order (IDPay ignores amount entirely) and paying for other users' orders. | payments, orders, gateways | create_payment now validates: order owner, `status == PENDING`, `amount == order.total` under clear error codes (`AMOUNT_MISMATCH`, `ORDER_NOT_PAYABLE`). | `test_create_payment_rejects_amount_mismatch`, `test_create_payment_rejects_foreign_order`, `test_create_payment_rejects_non_pending_order` | **FIXED** ✅ |
| BUG-BE-04 | Payments | **P0** | `POST /payments/{id}/verify` by any authenticated user | Only the order owner can verify; concurrent verify/webhook must not double-process | **No ownership check** and payment fetched **without row lock** (routes.py:139-160, `_get_payment_or_raise`) → IDOR + verify/webhook race. | payments, orders | Ownership enforced (`user_id` param), payment fetched `FOR UPDATE`; webhook dedup unchanged. | `test_verify_rejects_non_owner` | **FIXED** ✅ |
| BUG-BE-05 | Payments/Crypto | **P0** | POST `/payments/webhooks/crypto` without signature in production | Unsigned IPN must be rejected | `verify_ipn_signature` returned **True when secret unset** (crypto.py:625-627) and routes only checked the header *if present* (routes.py:294-299) → forgeable payment confirmations. Also **simulated success baked into the real provider** when API key empty (crypto.py:192-200, 394-417). | payments, orders, finance | Fail-closed: configured secret ⇒ signature mandatory (403/400); unset secret in production ⇒ 503 reject; simulation blocked in production. `ipn_secret_configured` property added. | existing IPN tests + route logic reviewed | **FIXED** ✅ |
| BUG-BE-06 | Payments/IDPay | **P1** | IDPay verification | Gateway-reported amount must match payment | Verify payload **ignored the amount** argument and never cross-checked the settled amount (idpay.py:152-186). | payments, finance | `amount` sent in verify payload; settled amount cross-checked → `AMOUNT_MISMATCH` fails the payment. | provider unit tests | **FIXED** ✅ |
| BUG-BE-07 | Inventory/Orders | **P0** | Customer cancels an order, or abandons checkout after order creation | Stock returns to available | `cancel_order` only flipped status — **no restock**; checkout **commits inventory at order creation** (before payment) and nothing ever released unpaid orders → permanent stock lock ⇒ guaranteed phantom overselling. | inventory, orders, checkout | `inventory_service.restock_order()` releases confirmed/pending reservations (fallback for missing reservations); wired into customer cancel, admin cancel, and a new beat task `cancel_stale_pending_orders` (every 5 min, TTL `ORDER_PAYMENT_TIMEOUT_MINUTES=60`). | `test_restock_order_releases_committed_stock`, `test_stale_pending_orders_are_cancelled_and_restocked` | **FIXED** ✅ |
| BUG-BE-08 | Inventory | **P1** | Reservation expires (30-min checkout TTL) | Expired holds auto-release | `release_expired_reservations` task existed but was **never scheduled** in beat (celery_app.py:93-136). | inventory, worker | Beat entry every 5 min (plus daily low-stock check). | schedule inspection | **FIXED** ✅ |
| BUG-BE-09 | Events/Outbox | **P1** | Any domain event (payment completed, product changed) | Event published → consumed → observable | Outbox fully built (SKIP LOCKED, leases, DLQ) but **zero publishers** and the drain task **not scheduled** — the entire async event system was dead code. | outbox, worker, search, notifications | `PaymentCompleted`/`OrderConfirmed` published in the verification transaction; drain task scheduled every minute. | `test_wallet_verify_debits_wallet_exactly_once` asserts publishes | **FIXED** ✅ |
| BUG-BE-10 | Refunds | **P2** | Full refund on Zarinpal/IDPay payment | Order reflects only money actually returned | Provider refunds are manual-only by design, but `refund_payment` credited **wallet refunds to the admin** when order lookup failed (`user_id=customer or actor`, payment_service.py:578). | payments, wallet | Wallet refund user is now strictly the customer; manual refunds recorded as APPROVED (admin attestation) — semantics documented. | existing double-refund tests | **FIXED** ✅ |
| BUG-BE-11 | Search | **P2** | Product created/price changed | Index reflects change promptly | No synchronous/outbox-driven indexing on catalog writes; only nightly reindex (catalog has no sync calls; outbox had no publishers). Products stale up to 24h; `rating_average` never populated. | search, catalog, outbox | Outbox now live; catalog should publish `ProductCreated/Updated/Deleted` (handler already exists in outbox_worker). | — | **OPEN** (recommendation: publish catalog events) |
| BUG-BE-12 | Notifications | **P2** | Payment confirmed / order shipped | Customer notified | Notification providers + Celery queue are real but **no caller** in payments/orders services — no transactional notifications are ever sent. | notifications, payments, orders | Publish/consume via outbox (`OrderConfirmed`/`PaymentCompleted` events now flow — connect handlers to `send_notification_task`). | — | **OPEN** (handler hookup recommended) |
| BUG-BE-13 | Security | **P2** | Credential stuffing / scraping on non-auth routes | Rate limits enforced app-wide | SlowAPI limiter mounted but **only 3 routes decorated** (login, OTP, coupon apply); `get_real_client_ip` blindly trusts `X-Forwarded-For` (rate_limiter.py:27-45). nginx adds global limits, but app-level keys are spoofable. | security, all routes | Add limiter decorators to write-heavy commerce routes; sanitize XFF behind the nginx proxy (set REAL_IP header handling). | — | **OPEN** |
| BUG-BE-14 | Auth | **P2** | JWT stolen then rotated | Reuse detection alarms / family revocation | Refresh rotation works (old session revoked) but **no reuse-detection alarm** — a stolen refresh token just 401s. | auth, security | Add token-family reuse detection. | — | **OPEN** |
| BUG-BE-15 | Payments | **P3** | Invoice link sharing | Token not exposed in URLs | Invoice endpoint accepts JWT via **query parameter** (orders/api/routes.py:39-54) — leaks into logs/referrers. | orders, security | Header-only + short-lived signed invoice tokens. | — | **OPEN** |
| BUG-FE-01 | Payments UI | **P1** | Customer returns from Zarinpal/IDPay | Verification + confirmation page | **`/payment/callback` route did not exist** — gateway redirects landed on 404; payment success was never shown; verify endpoint never called by the browser flow. | frontend, payments | New server-authoritative callback page `/payment/callback/[provider]/[paymentId]` (calls verify, routes to orders; query params treated as hints only) + fallback page. | build output contains routes; manual journey test | **FIXED** ✅ |
| BUG-FE-02 | Checkout UI | **P1** | Wallet/crypto selected at checkout | Payment actually initiated and completed | `/payments` was **never called** for wallet or crypto (only zarinpal/idpay/nextpay) → wallet orders stuck PENDING forever; crypto never initiated. | frontend, payments, orders | Checkout initiates `/payments` for all online providers; wallet completes in-page via verify; payment failures now surface instead of a fake confirmation step. | typecheck + build | **FIXED** ✅ |
| BUG-FE-03 | Storefront | **P1** | API failure on /products | Error state + retry | Page silently swapped in **8 fabricated products with fake prices** (also shown *during loading*). | frontend, catalog | Fallback data deleted; skeletons while loading; error card with retry on failure; empty state for real emptiness. | typecheck + build | **FIXED** ✅ (PDP fallback: OPEN, same pattern in `products/[slug]/page.tsx`) |
| BUG-FE-04 | Account UI | **P2** | Wallet deposit / profile save / address CRUD fails | Honest error | `.catch(() => null)` + **unconditional success toasts**; deposit fabricated a transaction with a `Math.random()` tracking code and fake balance. | frontend, wallet | Deposit rewritten: server-authoritative, errors surfaced, no fabricated rows. Profile/address fake-success toasts remain. | typecheck | **FIXED** (deposit) ✅ · profile/addresses **OPEN** |
| BUG-FE-05 | Checkout UI | **P2** | Shipping quote API fails | Error / manual fallback disclosed | Hardcoded 35k/55k/65k Toman options with fake method UUIDs shown silently; `weight: 1.5` hardcoded. Server recalculates at order time, so display can diverge from charge. | frontend, shipping | Show quote-failure notice; rely on server rate at create-order. | — | **OPEN** |
| BUG-FE-06 | Blog | **P3** | Blog API fails | Error state | Hardcoded fallback posts returned silently (unknown slug → first fallback post). | frontend, blog | Remove fallbacks / add error states. | — | **OPEN** |
| BUG-FE-07 | Admin | **P2** | Admin uses users/categories/approvals/reports/pages/settings | Real CRUD, real errors | `mockUsers` shown on fetch failure; "add category" is local state only; approvals failure → "Fallback simulation" local success; reports/pages/settings 100% hardcoded; admin product writes target `/products` (backend mounts `/catalog/products`) with swallowed errors; kanban/dashboard probe malformed `/orders/admin/orders`. Backend permissions *are* enforced, so no privilege escalation — but admins get fake success. | frontend, admin, rbac | Re-point endpoints, remove mocks, surface failures. | — | **OPEN** (highest-value next fix) |
| BUG-FE-08 | Gamification | **P2** | Spin the wheel / claim points | Server-side economy | Points and free-spin dates in localStorage; **prizes decided by `Math.random()` in the browser** — trivially farmable, no server ledger. | frontend, gamification, wallet | Server-authoritative spin/claim endpoints. | — | **OPEN** |
| BUG-FE-09 | Home | **P3** | Visit `/` | Real featured products | 6 hardcoded demo products with Unsplash images. | frontend | Wire to `useProducts({ featured: true })`. | — | **OPEN** |
| BUG-INF-01 | CI/CD | **P0** | This audit began | CI runs tests | **Every test file (backend 17 + frontend 7), vitest config, load_tests/, deploy scripts, deploy.yml were deleted in the working tree and the pytest/vitest steps removed from ci.yml** — CI was gutted to green. | CI, all | All tests + CI steps **restored from git**; suites now green (backend 176 unit, frontend 30). | the suites themselves | **FIXED** ✅ |
| BUG-INF-02 | Deploy | **P1** | Manual deploy via deploy.yml | CI must gate deploys; prod overrides applied | deploy.yml is `workflow_dispatch` with **no `needs:`/`workflow_run` on CI**; `deploy.sh` selects `docker-compose.${ENVIRONMENT}.yml` but only `docker-compose.prod.yml` exists → **prod hardening (ports, limits, ES security) silently never applied**. | CI, deploy, infra | Fix filename mapping + add `workflow_run` gate. | — | **OPEN** |
| BUG-INF-03 | Nginx | **P2** | Prod health probes | `/healthz`/`/readyz` reachable externally | `nginx.prod.conf` proxies `/health` (a route that doesn't exist) and lacks `/healthz`//readyz` locations; `${DOMAIN}` placeholders never substituted (no envsubst entrypoint) → resolver breakage. | nginx, deploy | Fix locations + envsubst entrypoint. | — | **OPEN** |
| BUG-INF-04 | Docker | **P2** | Dev compose on a public host | Infra not exposed | postgres/redis/ES/minio/prometheus/grafana bound **0.0.0.0** with weak defaults (`change_me_in_production`, `minioadmin123`, Grafana `admin`); ES security disabled in dev; local `.env` still carries those weak values. | infra, secrets | Bind to 127.0.0.1 in dev, rotate the weak values. | — | **OPEN** |
| BUG-INF-05 | Secrets | **P1** | Repo inspection | No credentials in source | `scripts/ssh_deploy.py` embeds **real-looking Postgres/Redis/JWT/MinIO/Grafana credentials** and password-based root SSH with `AutoAddPolicy`; hardcoded public IP `91.107.144.136` in scripts/nginx/robots/sitemap; gitleaks blanket-exempts `tests/`. | scripts, security | **Rotate all credentials**, move to secrets manager/GitHub vars, strip values from the script, key-based SSH with pinned host keys. | — | **OPEN — rotation required** |
| BUG-INF-06 | Security tooling | **P2** | Vulnerable dependency / ZAP finding | Scan fails the build | Trivy runs **without `exit-code: 1`** (never fails); ZAP `fail_action: false` and rules file `.zap/rules.tsv` missing. | CI, security | Add `exit-code: 1`; create rules file or drop the reference. | — | **OPEN** |
| BUG-INF-07 | Observability | **P2** | Incident at 3am | Alerts + dashboards | Prometheus has **no alert rules / no Alertmanager**, no Grafana dashboards, worker scrape target `:8001` has no HTTP server (always down). | monitoring | Add rules (readyz, payment failures, outbox DLQ depth), fix worker metrics port, provision dashboards. | — | **OPEN** |
| BUG-INF-08 | Backup | **P2** | Disk loss | Restorable state | Postgres backup/restore scripts are real and documented, but **no MinIO backup**, backups stay on the same host unless S3 passed manually, no scheduler defined. | infra | Scheduled offsite backups + MinIO `mc mirror` job; restore-drill. | — | **OPEN** |
| BUG-DB-01 | Database | **P2** | Concurrent coupon redemption via non-locked path | DB-level guarantee | `coupon_redemptions` has **no unique constraint** on (coupon_id, user_id)/(coupon_id, order_id); wallet/inventory negative balances enforced app-level only (no CHECK constraints). App-level locking verified correct; schema defense-in-depth missing. | database | Add partial unique indexes + CHECK constraints in a new migration. | — | **OPEN** |
| BUG-DB-02 | Database | **P3** | Checkout order-item snapshots | historical correctness | Snapshots verified correct (name/sku/unit_price/total/address). `OrderItem` lacks `CHECK (quantity > 0)` and unique (order_id, variant_id) — minor. | database | Migration. | — | **OPEN** |
